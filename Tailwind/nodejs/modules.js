// const stu = require('./students');
const {students,batch} = require('./students');

// console.log(`Students list in Modules module: ${stu}`);
// console.log(`Students list in Modules module: ${stu.students} and baches ${stu.batch}`);
console.log(`Students list in Modules module: ${students} and baches ${batch}`);